const headerPage = require('../pages/intro.page'); // Path to your HeaderPage file

describe('Header Links', () => {
    it('should open the login page when clicking the login link', () => {
        browser.url(''); // Replace with the correct URL of your app
        browser.pause(5000);
       
    });

    it('should click new user button', () => { // Correct arrow function syntax
        browser.url(''); // Replace with the correct URL of your app
        browser.pause(5000);
        
        
    });
    
});
